<%- include('../partials/header') %>

<div class="row justify-content-center">
  <div class="col-md-4">
    <div class="card shadow">
      <div class="card-body p-4">
        <div class="text-center mb-4">
          <i class="bi bi-flag-fill text-success" style="font-size: 3rem;"></i>
          <h3 class="mt-2">N2골프 로그인</h3>
          <p class="text-muted">NH투자증권 골프동호회</p>
        </div>

        <% if (error) { %>
        <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <form method="POST" action="/auth/login">
          <%- include('../partials/csrf') %>
          <div class="mb-3">
            <label class="form-label">이름</label>
            <input type="text" name="name" class="form-control" placeholder="예: 홍길동" required autofocus>
          </div>
          <div class="mb-3">
            <label class="form-label">비밀번호</label>
            <input type="password" name="password" class="form-control" required>
          </div>
          <button type="submit" class="btn btn-success w-100">
            <i class="bi bi-box-arrow-in-right"></i> 로그인
          </button>
        </form>

        <hr class="my-4">
        <p class="text-muted text-center small mb-0">
          초기 비밀번호는 1234 입니다.<br>
          로그인 후 비밀번호를 변경해주세요.
        </p>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
